#include <stdio.h>

int main()
{
    int temp;
    printf("Enter a temperature (celcius): ");
    scanf("%d°c", &temp);
    if (temp <= 100)
    {
        printf("temperature is Warm.");
    }
    else if (<= 100)
    {
        /* code */
    }
    
    else if (temp == 0)
    {
        printf("Number is zero.");
    }
    else
    {
        printf("Number is negative.");
    }
    return 0;
}
